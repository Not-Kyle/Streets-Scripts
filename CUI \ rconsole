local function PrintCommands()
    if rconsoleprint then 
        rconsolename("Title") --Change this to your script name
        wait(1.3)--You can take this out
        rconsoleprint("Hello uh "..game.Players.LocalPlayer.Name) --you can take out the name command
        
end
end
PrintCommands()
