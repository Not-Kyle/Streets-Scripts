local METAVAR = {['Mouse'] = game:GetService('Players').LocalPlayer:GetMouse()};

METAVAR['Mouse'].KeyDown:connect(function(framekey)
    if framekey:lower() == 'z' then
    --Frame.Visible = not Frame.Visible  (Change the name Frame, to your frames name)
    end
end)
