local function PrintCommands()
    if rconsoleprint then 
        rconsolename("Title")
        wait(1.3)
        rconsoleprint("Hello uh "..game:GetService'Players'.LocalPlayer.Name)
    end
end
PrintCommands()
